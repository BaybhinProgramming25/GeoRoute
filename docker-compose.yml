name: simplemaps
services:

  frontend:
    image: simplemaps-frontend
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - "5173:80"
    depends_on:
      - backend
    networks:
      - simplemaps

  backend:
    image: simplemaps-backend
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    env_file:
      - ./backend/.env
    depends_on:
      - postgres
      - redis
    networks:
      - simplemaps

  postgres:
    image: pgrouting/pgrouting:latest
    container_name: simplemaps_postgres
    command:
      - "postgres"
      - "-c"
      - "shared_buffers=256MB"
      - "-c"
      - "max_connections=100"
      - "-c"
      - "work_mem=16MB"
      - "-c"
      - "maintenance_work_mem=128MB"
    ports:
      - "5432:5432"
    env_file:
      - backend/.env
    volumes:
      - postgres-data:/var/lib/postgresql
      - ./backend/db/init.sql:/docker-entrypoint-initdb.d/init.sql
    restart: unless-stopped
    networks:
      - simplemaps
  
  osm-routing-import:
    image: iboates/osm2pgsql:latest
    volumes:
      - ./data/region.osm.pbf:/data/region.osm.pbf
    env_file:
      - ./backend/.env
    environment:
      - PGPASSWORD=${POSTGRES_PASSWORD}
    command: osm2pgsql --slim -d routing_db -H postgres -U ezmapsuser /data/region.osm.pbf
    depends_on:
      - postgres
    profiles:
      - routing-import
    networks:
      - simplemaps
      
  redis:
    image: redis:alpine
    ports:
      - "6379:6379"
    volumes:
      - redis-data:/data
    networks:
      - simplemaps


volumes:
  postgres-data:
  redis-data:
  osm-data:
networks:
  simplemaps:
